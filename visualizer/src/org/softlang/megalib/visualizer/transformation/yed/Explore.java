package org.softlang.megalib.visualizer.transformation.yed;

public class Explore {

	public static void main(String[] args) {
		/*UndirectedGraph<String, DefaultWeightedEdge> g = new SimpleGraph<String, DefaultWeightedEdge>(DefaultWeightedEdge.class);

		String v1 = "v1";
		String v2 = "v2";
		String v3 = "v3";
		String v4 = "v4";

		// add the vertices
		g.addVertex(v1);
		g.addVertex(v2);
		g.addVertex(v3);
		g.addVertex(v4);

		// add edges to create a circuit
		DefaultWeightedEdge e = g.addEdge(v1, v2);
		
		g.addEdge(v2, v3);
		g.addEdge(v3, v4);
		g.addEdge(v4, v1);

		FileWriter w;
		try {
			GraphMLExporter<String, DefaultWeightedEdge> exporter = new GraphMLExporter<>((v) -> v, v -> "Hello", new IntegerComponentNameProvider<>(), null);
            
		    w = new FileWriter("test.graphml");
		    exporter.exportGraph(g, w);
		} catch (IOException ex) {
		    ex.printStackTrace();
		} catch (ExportException ex) {
			// TODO Auto-generated catch block
			ex.printStackTrace();
		}
		*/
	}
}
